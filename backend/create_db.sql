-- SQL script to create the database
-- Run this after PostgreSQL is installed
-- Usage: psql -U postgres -f create_db.sql

CREATE DATABASE skykeen_db;

-- Optional: Create a dedicated user (recommended for production)
-- CREATE USER skykeen_user WITH PASSWORD 'your_secure_password';
-- GRANT ALL PRIVILEGES ON DATABASE skykeen_db TO skykeen_user;

